<!DOCTYPE html>
<html lang="en">
{% load static %}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Django App</title>
    <style>
        :root {
            --primary-red: #e63946;
            --light-red: #ff6b6b;
            --dark-red: #d00000;
            --white: #f1faee;
            --off-white: #f8f9fa;
            --glass-color: rgba(240, 240, 245, 0.15);
            --glass-border: 1px solid rgba(255, 255, 255, 0.3);
            --glass-blur: blur(15px);
            --text-color: #333;
            --border-radius: 16px;
            --accent-color: rgba(230, 57, 70, 0.85);
            --dark-overlay: rgba(60, 60, 70, 0.3);
        }
        
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-image: url("{% static 'lmc.jpg' %}");
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: var(--text-color);
            position: relative;
        }
        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            backdrop-filter: blur(1px);
            background-color: var(--dark-overlay);
            z-index: -1;
        }
        
        .header {
            background: var(--primary-red);
            color: white;
            display: flex;
            align-items: center;
            padding: 1rem 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 10;
        }
        
        .logo {
            height: 50px;
            width: 50px;
            background-color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.5rem;
            color: var(--primary-red);
            margin-right: 1.5rem;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.8);
        }
        
        .header h1 {
            margin: 0;
            font-size: 2.2rem;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            flex-grow: 1;
        }
        
        .navbar {
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }
        
        .navbar ul {
            list-style: none;
            display: flex;
            justify-content: center;
            padding: 0;
            margin: 0;
        }
        
        .navbar li {
            margin: 0 5px;
        }
        
        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 600;
            padding: 6px 16px;
            border-radius: 20px;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .navbar a:hover {
            background: white;
            color: var(--primary-red);
            transform: translateY(-2px);
        }
        
        .container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
            flex: 1;
            z-index: 1;
        }
        
        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }
        
        .feature-card {
            background: var(--glass-color);
            backdrop-filter: var(--glass-blur);
            -webkit-backdrop-filter: var(--glass-blur);
            border: var(--glass-border);
            border-radius: var(--border-radius);
            padding: 1.5rem;
            color: white;
            text-align: center;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 250px;
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
        }
        
        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--white);
        }
        
        .feature-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--white);
        }
        
        .feature-text {
            font-size: 1rem;
            line-height: 1.6;
            color: var(--off-white);
        }
        
        .hero {
            text-align: center;
            padding: 3rem 1rem;
            background: var(--accent-color);
            border-radius: var(--border-radius);
            margin-bottom: 3rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
        }
        
        .hero h2 {
            font-size: 2.5rem;
            color: white;
            margin-bottom: 1rem;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }
        
        .hero p {
            font-size: 1.2rem;
            color: var(--off-white);
            max-width: 800px;
            margin: 0 auto 2rem;
        }
        
        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        
        .btn-primary {
            background: white;
            color: var(--primary-red);
        }
        
        .btn-primary:hover {
            background: var(--off-white);
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }
        
        .footer {
            background: var(--primary-red);
            color: white;
            text-align: center;
            padding: 1.5rem;
            margin-top: auto;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">UM</div>
        <h1>User Management</h1>
        <div class="navbar">
            <ul>
                <li><a href="{% url 'index' %}">Home</a></li>
                <li><a href="{% url 'home' %}">Register</a></li>
                <li><a href="{% url 'show_form' %}">User List</a></li>
            </ul>
        </div>
    </div>
    
    <div class="container">
        <div class="hero">
            <h2>Welcome to User Management System</h2>
            <p>A modern and efficient way to manage user data with Django</p>
            <a href="{% url 'home' %}" class="btn btn-primary">Get Started</a>
        </div>
        
        <div class="feature-grid">
            <div class="feature-card">
                <div class="feature-icon">📝</div>
                <h3 class="feature-title">Easy Registration</h3>
                <p class="feature-text">Quick and simple user registration process with minimal fields required.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">👥</div>
                <h3 class="feature-title">User Management</h3>
                <p class="feature-text">Efficiently manage all your users in one place with our intuitive interface.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🔄</div>
                <h3 class="feature-title">Update Profiles</h3>
                <p class="feature-text">Easily update user information with our streamlined editing process.</p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">🔒</div>
                <h3 class="feature-title">Secure Storage</h3>
                <p class="feature-text">All user data is securely stored and protected in our database.</p>
            </div>
        </div>
    </div>
    
    <div class="footer">
        <p>made with &hearts;</p>
    </div>
</body>
</html>
