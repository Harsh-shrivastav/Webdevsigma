<!DOCTYPE html>
<html lang="en">
{% load static %}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Profile</title>
    <style>
        :root {
            --primary-red: #e63946;
            --light-red: #ff6b6b;
            --dark-red: #d00000;
            --white: #f1faee;
            --off-white: #f8f9fa;
            --glass-color: rgba(240, 240, 245, 0.15);
            --glass-border: 1px solid rgba(255, 255, 255, 0.3);
            --glass-blur: blur(15px);
            --text-color: #333;
            --border-radius: 16px;
            --accent-color: rgba(230, 57, 70, 0.85);
            --dark-overlay: rgba(60, 60, 70, 0.3);
        }
        
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-image: url("{% static 'lmc.jpg' %}");
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: var(--text-color);
            position: relative;
        }
        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            backdrop-filter: blur(1px);
            background-color: var(--dark-overlay);
            z-index: -1;
        }
        
        .header {
            background: var(--primary-red);
            color: white;
            display: flex;
            align-items: center;
            padding: 1rem 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 10;
        }
        
        .logo {
            height: 50px;
            width: 50px;
            background-color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.5rem;
            color: var(--primary-red);
            margin-right: 1.5rem;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.8);
        }
        
        .header h1 {
            margin: 0;
            font-size: 2.2rem;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            flex-grow: 1;
        }
        
        .navbar {
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }
        
        .navbar ul {
            list-style: none;
            display: flex;
            justify-content: center;
            padding: 0;
            margin: 0;
        }
        
        .navbar li {
            margin: 0 5px;
        }
        
        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 600;
            padding: 6px 16px;
            border-radius: 20px;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .navbar a:hover {
            background: white;
            color: var(--primary-red);
            transform: translateY(-2px);
        }
        
        .container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
            flex: 1;
            z-index: 1;
        }
        
        form {
            background: rgba(245, 245, 250, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            border-radius: var(--border-radius);
            padding: 2rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }
        
        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--primary-red);
        }
        
        input {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(200, 200, 200, 0.8);
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.8);
            font-size: 1rem;
        }
        
        input:focus {
            outline: none;
            border-color: var(--primary-red);
            box-shadow: 0 0 0 3px rgba(230, 57, 70, 0.2);
        }
        
        button {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-right: 1rem;
        }
        
        button[type="submit"] {
            background: var(--primary-red);
            color: white;
            box-shadow: 0 4px 15px rgba(230, 57, 70, 0.3);
        }
        
        button[type="submit"]:hover {
            background: var(--dark-red);
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(230, 57, 70, 0.4);
        }
        
        button[type="reset"] {
            background: rgba(200, 200, 200, 0.8);
            color: #444;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        
        button[type="reset"]:hover {
            background: rgba(180, 180, 180, 0.9);
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
        }
        
        .footer {
            background: var(--primary-red);
            color: white;
            text-align: center;
            padding: 1.5rem;
            margin-top: auto;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">UM</div>
        <h1>Update Profile</h1>
        <div class="navbar">
            <ul>
                <li><a href="{% url 'index' %}">Home</a></li>
                <li><a href="{% url 'home' %}">Register</a></li>
                <li><a href="{% url 'show_form' %}">User List</a></li>
            </ul>
        </div>
    </div>
    
    <div class="container">
        <form action="{% url 'update_submit' data.id %}" method="post">
            {% csrf_token %}
            <label for="name">Name:</label>
            <input type="text" id="name" name="username" value="{{ data.username }}" placeholder="Enter your name" required>
            <label for="email">Email:</label>
            <input type="text" id="email" name="email" value="{{ data.email }}" placeholder="Enter your email" required>
            <label for="password">Password:</label>
            <input type="text" id="password" name="password" value="{{ data.password }}" placeholder="Enter your password" required>
            <button type="submit">Update</button>
            <button type="reset">Reset</button>
        </form>
    </div>
    
    <div class="footer">
        <p>made with &hearts;</p>
    </div>
</body>
</html>
