<!DOCTYPE html>
<html lang="en">
{% load static %}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="min-h-screen">
    <!-- Header -->
    <header class="bg-gray-700 shadow flex items-center justify-between p-4 border-b-2 border-blue-200">
        <div class="flex items-center space-x-4">
            <img src="{% static 'logo.jpg' %}" alt="Logo" class="h-10 w-10 rounded-full">
            <span class="text-2xl font-bold text-white">User Management</span>
        </div>
        <nav class="flex space-x-6">
            <a href="{% url 'index' %}" class="text-white hover:text-blue-600 font-medium">Home</a>
            <a href="{% url 'home' %}" class="text-white hover:text-blue-600 font-medium">Register</a>
            <a href="{% url 'show_form' %}" class="text-white hover:text-blue-600 font-medium">User List</a>
        </nav>
    </header>

    <!-- Background Section -->
    <div class="bg-[url('{% static 'lmc.jpg' %}')] relative min-h-screen bg-cover bg-center">
        <!-- Overlay -->
        <div class="absolute inset-0 bg-black bg-opacity-30"></div>
        
        <!-- Content -->
        <div class="relative z-10 px-4 py-8">
            <div class="max-w-6xl mx-auto">
                <div class="bg-white bg-opacity-90 backdrop-blur-sm rounded-2xl shadow-2xl p-8">
                    <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">Registered Users</h2>
                    
                    {% if data %}
                    <div class="overflow-x-auto">
                        <table class="w-full table-auto">
                            <thead>
                                <tr class="bg-gray-100">
                                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-700 border-b">ID</th>
                                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-700 border-b">Username</th>
                                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-700 border-b">Email</th>
                                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-700 border-b">Password</th>
                                    <th class="px-4 py-3 text-center text-sm font-medium text-gray-700 border-b">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for row in data %}
                                <tr class="hover:bg-gray-50 transition-colors duration-200">
                                    <td class="px-4 py-3 text-sm text-gray-800 border-b">{{ row.id }}</td>
                                    <td class="px-4 py-3 text-sm text-gray-800 border-b">{{ row.username }}</td>
                                    <td class="px-4 py-3 text-sm text-gray-800 border-b">{{ row.email }}</td>
                                    <td class="px-4 py-3 text-sm text-gray-800 border-b">{{ row.password }}</td>
                                    <td class="px-4 py-3 text-center border-b">
                                        <div class="flex justify-center space-x-2">
                                            <a href="{% url 'update_form' row.id %}" 
                                               class="bg-blue-500 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-600 transition duration-200">
                                                Update
                                            </a>
                                            <a href="{% url 'delete_form' row.id %}" 
                                               class="bg-red-500 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-600 transition duration-200"
                                               onclick="return confirm('Are you sure you want to delete this user?')">
                                                Delete
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-12">
                        <div class="text-6xl text-gray-300 mb-4">📋</div>
                        <h3 class="text-xl font-medium text-gray-600 mb-2">No Users Found</h3>
                        <p class="text-gray-500 mb-6">There are no registered users yet.</p>
                        <a href="{% url 'home' %}" 
                           class="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition duration-200">
                            Register First User
                        </a>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-700 text-white text-center py-4">
        <p>made with ❤️</p>
    </footer>
</body>
</html>
